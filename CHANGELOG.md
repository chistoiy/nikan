# 更新日志

本项目遵循「一次发布 = 一个 tag」的方式记录。详细的功能说明与可直接发布的公告文案见
`docs/更新说明-1.0.1.md`。

## 1.0.2（2026-09-18）

来源：`docs/体检报告-2026-09-16.md`（对 2026-09-15 代码审查 18 项条目的逐条复核）的修复批次 1~3，
外加「无线下载提速研究」的探针与结论文档。versionCode 基号 2042（arm64=4042 / arm32=3042 / x86_64=6042）；
`flutter analyze` 0 问题、`flutter test` 26 项全过。

### 修复（多数是静默的数据/体验缺陷）

- **竖拍照片下载成 8M/2M 档后会变成横的**：缩放改用 `ImageDecoder.setTargetSize`，
  它会自动应用 EXIF 方向，同时按目标尺寸解码（内存峰值也从约 82MB 大幅下降）。
  原图档不受影响——这条此前一直是静默的，用户拿到横躺的照片不会想到是 App 干的。
- **「取消下载」对大文件无效**：取消标志下沉到传输层（Wi-Fi 分块边界 / USB 读块边界），
  不再要等整个文件传完；取消与"失败"在结果里彻底分开，文案改成"已取消（已下载 N，剩余 M 张未传）"。
- **USB 下载期间保活失效**：取证确认事务锁不能按块释放（整文件下载是单笔 GetObject 的数据相位），
  改为下载期间不发探针、用"最近是否还有进度"判活性（停滞 30 秒才判可疑）——既不再堆积无用事务，
  又能真正发现断线。
- **索引进度通知条件写错**：原来比较的是列表总长度（常量）而非已补全详情的数量，
  1500 张的卡上条件恒为假、进度靠副作用刷新。
- **每次模型通知都重算筛选与排序**：改为指纹比较（含已索引数量），相册页与手机页都改。
- 保活通知此前用的是**系统蓝牙图标**，且 `POST_NOTIFICATIONS` 一直没在运行时申请
  （Android 13+ 上通知完全不可见）——两者都已修。
- **插入 USB 相机时应用被拉起却什么都不做**：补上 Intent 处理，连接页会切到 USB 模式并提示。
- `build_apk.bat` 的路径里有 0x07 控制字符（`\a` 被写坏），且两个脚本只编 debug——
  已重写：支持 release、基号自 `.build_number` 自增、产物与核对命令都会打印。

### 优化

- 查看器按显示尺寸解码（分常态/放大两档、只缩不放、单张上限约 4K 宽），避免单张 82MB 位图导致 OOM。
- 单元格 GlobalKey 与缩略图缓存随列表变化回收；系统内存压力时释放缩略图内存缓存。
- 删除死代码（`CameraFile.dedupKey`，其实现本身也有 bug：`$sizeOverride` 会拼出 `null`）。

### 工程

- 新增**「无线测速」探针**（调试面板）：把选中文件真传一遍但丢弃数据，报告平均吞吐、
  最慢区间、当前频段（2.4/5GHz）与协商速率，并直接判读"还有没有提速空间"。
  用途：同一文件在「相机 AP 模式」与「相机 STA 模式（接入 5GHz 路由器）」各跑一次即可定瓶颈。
- 链路日志补上**频段**（`… 速率 54 Mbps · 2.4GHz`）：54Mbps 即 802.11g，不写频段无法判断上限。
- 无线提速研究结论文档化：`docs/无线下载提速研究.md`（AP 模式已接近 11g 上限，
  有量级意义的只有 5GHz/STA 模式与 USB）。
- 设置引入 `schemaVersion` + 迁移函数，键名去后缀（不再靠 `xxV2/V3` 演进）。
- `docs/技术方案.md` 顶部补「实现与方案的差异」（Riverpod→ChangeNotifier、drift→JSON 等）。
- `docs/AI交接文档.md` §7/§11 重写：标清哪些问题已解决、哪些"不可为"，并**纠正原文错误的属性码映射**
  （正确为 `0x5007` 光圈 / `0x500D` 快门 / `0x500E` 档位 / `0x500F` ISO）。
- `dist/` 从 293MB 清到 82MB（只留发布版）。

## 1.0.1

> 基线：`7d9980d`（2026-09-14 发布签名与最小体积 release 构建）。
> 统计范围：`7d9980d` 之后到本版发布为止的全部改动。

### 新增

- **USB 数据线连接**：与 Wi-Fi 共用同一套相册/下载/遥控逻辑，实测 **27.1 MB/s**（Wi-Fi 智能设备模式的 11 倍）。
- **连接页重排**：Wi-Fi / USB 一键切换，连接按钮直接可见，操作步骤收进可展开区。
- **相册**：按住滑动批量选择；按拍摄日期分组、点日期头即整选当天；只看未下载；待下载任务条与一键补下。
- **大图查看器**：低 / 中 / 原图三档画质；「显示原图」有真实进度（百分比·已接收·速率·已用时）与停滞警示；可选把原图一并存到手机。
- **遥控拍摄**：光圈 / 快门 / ISO / 曝光补偿可直接在手机上改；点取景画面指定对焦点；盲拍模式（不进取景也可以拍）。
- **RAW+JPEG 成对**：自动识别同名成对文件，相册显示 `R+J` 角标与 `J✓ R✓` 下载状态；勾选一张自动带上另一半（可关）；筛选条新增「成对」。
- **链路可观测**：Wi-Fi 信号格与相机活性分开显示，点开可看诊断（响应耗时、距上次消息时间、速率）。
- **应用图标**：新的启动图标（同步环 + 镜头 + 对焦点），含自适应图标与主题图标。

### 修复

- **Wi-Fi 下修改相机参数一定会失败**：根因是 PTP/IP「数据外发」报文的事务号字段写成了偏移量，相机一律回 `TransactionCancelled`。改为按参考实现逐字段构造后，光圈/快门/ISO/补偿都能改。
- **相机息屏后遥控失效**：现在能识别「相机已进入待机」，遥控页给出明确提示与恢复入口，并说明长期方案（相机菜单「电源关闭延迟」）。
- **空闲后假掉线**：单次读超时不再作废整条连接（区分「超时落在包边界」与「半个包残留」）。
- **保活探针发错操作码**：0x90C1 其实是「AF 驱动」，等于每 5 秒驱动一次对焦；已改为只读探测。
- **对焦优先机型拍不了**：先合焦再按快门，重试不再打断 AF；失败文案给出相机菜单路径。
- **点取景画面指定对焦点位置对不上**：对焦倍数改为**从取景帧头部自动解出**（相机图像尺寸 ÷ 取景帧尺寸，x/y 各一次），不再依赖人工标定。
- **USB 路径三个硬 bug**：读缓冲处理、目录枚举递归、大文件传输；修完实测 27.1 MB/s。
- **相册状态条互相堆叠**：三条状态条合并为一条（按优先级），不再挤掉一行网格。
- **重复连接会打断相机**、连接状态在两个入口之间不一致。
- **下载完整性**：传输量必须与声明一致，不一致就显式失败，不静默留下半个文件。

### 已知限制

- 相机进入待机（屏幕熄灭）后取景与快门都会被拒；本机（Z50 II · Wi-Fi 智能设备模式）**不提供**任何息屏/待机属性，App 改不了，请在相机菜单把「电源关闭延迟」调长。
- Wi-Fi 智能设备模式速率上限约 1.5~2.4 MB/s，大批量传输请用 USB。
- RAW 文件在 App 内只有缩略图（无法在手机上看 RAW 原图）。
- 目前仅在 **Nikon Z50 II（固件 1.02）** 上完整验证。

---

## 1.0.0

首个可用版本：Wi-Fi 智能设备模式下的相册浏览、断点续传下载、缩略图缓存、
相机端保护/删除、下载去重记录与 release 签名构建。
